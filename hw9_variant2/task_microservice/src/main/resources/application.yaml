spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kaminskiy1987-db
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  application:
    name: taskService

server:
  port: 8081
eureka:
  instance:
    hostname: localhost