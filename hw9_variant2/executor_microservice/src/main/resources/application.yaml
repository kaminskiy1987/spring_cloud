spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kaminskiy1987-db
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  application:
    name: executorService

server:
  port: 8082
eureka:
  instance:
    hostname: localhost